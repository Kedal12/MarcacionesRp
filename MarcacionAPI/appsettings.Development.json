{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://0.0.0.0:5000" // Puerto HTTP estándar para Kestrel
      },
      "Https": {
        // Mantenemos HTTPS por si lo necesitas, pero usaremos HTTP para Expo Go
        "Url": "https://0.0.0.0:7042" // Tu puerto HTTPS actual
      }
    }
  },
  "AllowedHosts": "*" // Permite conexiones desde cualquier host (adecuado para desarrollo local)
}